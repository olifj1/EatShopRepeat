# Meal Planner v1.0.10

Small split-meal layout consistency pass.

- Split meal names now use the same size, weight and left alignment as normal unsplit meal names.
- The `All` dropdown chevron is now a properly centred inline SVG, so the label and arrow sit on the same visual baseline.
- No household, meal-plan or shopping data is reset by this update.
